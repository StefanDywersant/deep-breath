datasource:
  dacsystem:
    redis:
      host: 'localhost'
      port: 6379
      prefix: 'deep-breath:tst:'

store:
  webserver:
    port: 8091
    hostname: 'localhost'

mobile:
  webserver:
    port: 8090
    hostname: 'localhost'

aqi:
  webserver:
    port: 8092
    hostname: 'localhost'
