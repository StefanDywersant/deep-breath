datasource:
  pl-wielkopolskie:
    redis:
      host: 'localhost'
      port: 6379
      prefix: 'deep-breath:dev:pl-wielkopolskie:'

store:
  webserver:
    port: 8081

mobile:
  webserver:
    port: 8080

  store:
    host: 'localhost'
    port: 8081

